@model IEnumerable<EventsPlanning.Models.ApplicationUser>
@{
    ViewBag.Title = "Список пользователей";
}

<h2>Список пользователей</h2>
<table class="table">
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <a href="/Roles/Edit?userId=@user.Id">Права доступа</a>
            </td>
        </tr>
    }
</table>